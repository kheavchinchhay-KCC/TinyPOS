import { useEffect,useState } from "react";import { X } from "lucide-react";import { INTEGRATION_EVENTS } from "../lib/integrations";
const defaults={name:"",description:"",endpoint_url:"",event_types:["sale.completed"],branch_ids:[],timeout_seconds:10,max_attempts:8,is_active:true};
export default function WebhookEndpointModal({open,branches,busy,onClose,onSave}){const[v,setV]=useState(defaults);useEffect(()=>{if(open)setV(defaults)},[open]);if(!open)return null;const toggle=(key,value)=>setV(c=>({...c,[key]:c[key].includes(value)?c[key].filter(x=>x!==value):[...c[key],value]}));return <div className="modal-backdrop"><div className="modal integration-modal"><div className="modal-header"><div><p className="eyebrow">SIGNED OUTGOING EVENTS</p><h2>New Webhook</h2></div><button className="icon-button" onClick={onClose}><X size={20}/></button></div><div className="integration-form-grid"><label>Name<input value={v.name} onChange={e=>setV({...v,name:e.target.value})}/></label><label>HTTPS endpoint<input value={v.endpoint_url} onChange={e=>setV({...v,endpoint_url:e.target.value})} placeholder="https://example.com/webhooks/tiny-pos"/></label><label>Timeout seconds<input type="number" min="3" max="30" value={v.timeout_seconds} onChange={e=>setV({...v,timeout_seconds:e.target.value})}/></label><label>Maximum attempts<input type="number" min="1" max="12" value={v.max_attempts} onChange={e=>setV({...v,max_attempts:e.target.value})}/></label><label className="span-2">Description<textarea value={v.description} onChange={e=>setV({...v,description:e.target.value})}/></label></div><h3>Events</h3><div className="integration-check-grid">{INTEGRATION_EVENTS.map(event=><label className="check-card" key={event}><input type="checkbox" checked={v.event_types.includes(event)} onChange={()=>toggle("event_types",event)}/><span><strong>{event}</strong></span></label>)}</div><h3>Branches</h3><p className="muted">Leave all unchecked to receive events from all branches.</p><div className="integration-check-grid">{branches.map(b=><label className="check-card" key={b.id}><input type="checkbox" checked={v.branch_ids.includes(b.id)} onChange={()=>toggle("branch_ids",b.id)}/><span><strong>{b.name}</strong><small>{b.code}</small></span></label>)}</div><label className="form-check"><input type="checkbox" checked={v.is_active} onChange={e=>setV({...v,is_active:e.target.checked})}/><span>Webhook is active</span></label><div className="modal-actions"><button className="secondary" onClick={onClose}>Cancel</button><button disabled={busy||!v.name.trim()||!v.endpoint_url.trim()||!v.event_types.length} onClick={()=>onSave({...v,timeout_seconds:Number(v.timeout_seconds),max_attempts:Number(v.max_attempts)})}>{busy?"Creating…":"Create webhook"}</button></div></div></div>}
