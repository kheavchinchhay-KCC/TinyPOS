import { useState } from "react";import { Check,Copy,X } from "lucide-react";
export default function SecretRevealModal({open,title,secret,help,onClose}){const[copied,setCopied]=useState(false);if(!open)return null;async function copy(){await navigator.clipboard.writeText(secret||"");setCopied(true);setTimeout(()=>setCopied(false),1500)}return <div className="modal-backdrop"><div className="modal secret-modal"><div className="modal-header"><div><p className="eyebrow">SHOWN ONCE</p><h2>{title}</h2></div><button className="icon-button" onClick={onClose}><X size={20}/></button></div><p>{help||"Copy this secret now. Tiny POS cannot show it again."}</p><div className="secret-reveal"><code>{secret}</code><button onClick={copy}>{copied?<Check size={18}/>:<Copy size={18}/>} {copied?"Copied":"Copy"}</button></div><div className="warning-box">Store this value in the external system&apos;s secret manager. Do not place it in React, GitHub, screenshots, or public chat.</div><div className="modal-actions"><button onClick={onClose}>I saved it</button></div></div></div>}
